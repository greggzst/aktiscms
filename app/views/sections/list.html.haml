- cache @sections do
  - @sections.each do |section|
    - cache section do
      .row{:id => "#{section.refname}"}
        %h1.section-name= section.name
        .elements
          = render section.section_elements